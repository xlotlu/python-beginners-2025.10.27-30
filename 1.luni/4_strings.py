# strings

# often used

## return new strings
s.replace()

s.lower() / s.upper()

s.strip() / s.lstrip() / s.rstrip() # cleanup

## return integers
s.find() / s.index() # diferența: not found --> -1 / ValueError

s.count()


## return boolean
s.startswith() / s.endswith()


## utilitare esențiale
s.split / s.rsplit  <--> s.join


s.splitlines




## TODO!
# s.encode 


# sometimes used
s.ljust() / s.rjust()



# sometimes used, return boolean
s.isalpha() / .isdecimal() / .isidentifier() / .isprintable()

# rarely used, return new strings
s.capitalize / s.title()


# rarely used, return boolean
s.isupper() / s.islower() / s.istitle()
